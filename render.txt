services:
  - type: web
    name: simulation-traffic-flow-optimization
    env: python
    plan: free
    buildCommand: |
      sudo apt-get update
      sudo apt-get install -y libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev
      pip install -r requirements.txt
      pip install pygame
    startCommand: gunicorn app:app
